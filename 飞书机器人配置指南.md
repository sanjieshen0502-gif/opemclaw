# 飞书机器人配置指南（完整版）

## 🎯 配置目标
让OpenClaw能够接收和回复飞书私聊消息

## 📋 所需时间：约15-20分钟

---

## 第1步：登录飞书开放平台

1. **打开网址**：[open.feishu.cn](https://open.feishu.cn)
2. **登录账号**：使用你的飞书账号登录
3. **进入控制台**：点击右上角“开发者后台”

**截图指引**：
```
登录后应看到“应用管理”或“我的应用”
```

---

## 第2步：创建应用

1. **创建企业自建应用**
   - 点击“创建企业自建应用”
   - 应用名称：`OpenClaw助手`（可自定义）
   - 应用描述：`OpenClaw AI助手集成`（可自定义）
   - 点击“确认创建”

2. **进入应用详情页**
   - 保存**应用凭证**（下一步需要）

**截图指引**：
```
需要截图：应用详情页的“凭证与基础信息”部分
重点：App ID、App Secret
```

---

## 第3步：获取应用凭证（关键！）

在应用详情页找到以下信息，**复制到记事本暂存**：

### 🔑 必须获取的凭证
| 项目 | 位置 | 示例格式 |
|------|------|----------|
| **App ID** | 凭证与基础信息 → App ID | `cli_xxxxxxxxxxxxxx` |
| **App Secret** | 凭证与基础信息 → App Secret | `xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx` |
| **Verification Token** | 事件订阅 → Verification Token | `xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx` |

**重要提示**：
- App Secret **只显示一次**，务必立即复制保存！
- Verification Token 在“事件订阅”页面生成/查看

---

## 第4步：配置事件订阅（最重要！）

### 4.1 开启事件订阅
1. 左侧菜单 → “事件订阅”
2. 点击“启用”按钮

### 4.2 配置请求地址（Webhook URL）
你的OpenClaw网关地址通常是：
```
http://localhost:3000/webhook/feishu
```

**填写到飞书**：
- 请求地址：`http://localhost:3000/webhook/feishu`
- 如果OpenClaw运行在其他地址，请相应调整

### 4.3 添加事件权限
点击“添加事件”，选择以下权限：
1. **接收消息**（必须！）
   - `im:message`
   - `im:message.group_at_msg` (可选，群聊@消息)
   - `im:message.p2p_msg` (必须，私聊消息)

### 4.4 保存并验证
1. 点击“保存”
2. 飞书会发送验证请求
3. **OpenClaw必须运行**才能验证成功

**截图指引**：
```
需要截图：事件订阅页面
重点：请求地址、已添加的事件权限
```

---

## 第5步：配置环境变量

### 5.1 打开配置文件
打开文件：`C:\Users\Komorebi\.openclaw\.env`

### 5.2 添加飞书配置
在文件末尾添加（**替换成你的真实凭证**）：

```env
# 飞书配置
FEISHU_APP_ID=cli_xxxxxxxxxxxxxx
FEISHU_APP_SECRET=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
FEISHU_VERIFICATION_TOKEN=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
```

### 5.3 保存文件

**安全提醒**：
- `.env` 文件包含敏感信息，**不要分享**！
- 不要在聊天中发送完整凭证

---

## 第6步：重启网关

### 方法A：命令行重启
```powershell
# 打开PowerShell或CMD
openclaw gateway restart
```

### 方法B：如果方法A失败
```powershell
# 1. 停止网关
openclaw gateway stop

# 2. 等待10秒

# 3. 启动网关  
openclaw gateway start
```

### 等待启动完成
网关重启需要10-30秒，看到类似消息表示成功：
```
Gateway started (pid: xxxxx)
```

---

## 第7步：测试连接

### 测试1：飞书平台验证
1. 回到飞书开放平台
2. 在“事件订阅”页面
3. 查看请求地址旁边是否有 **绿色对勾** ✓
   - 绿色：验证成功
   - 红色：验证失败（检查第4-6步）

### 测试2：发送测试消息
在飞书里：
1. **搜索你的机器人**（应用名称）
2. **开始私聊**
3. 发送消息：`@机器人 测试123`
4. 等待回复（应该能看到“已读”标记）

### 测试3：检查OpenClaw日志
```powershell
# 查看最近日志
openclaw log --tail 20
```
如果看到类似消息，说明飞书消息已收到：
```
[feishu] Received message from user: xxxxx
```

---

## 🔧 常见问题解决

### ❌ 问题1：验证失败（红色×）
**可能原因**：
1. OpenClaw网关没运行
2. 端口被占用（不是3000）
3. 防火墙阻止

**解决方案**：
1. 确认网关运行：`openclaw gateway status`
2. 检查端口：`netstat -ano | findstr :3000`
3. 临时关闭防火墙测试

### ❌ 问题2：消息未读/未回复
**可能原因**：
1. 事件权限没加对
2. 私聊权限没开启
3. 环境变量没生效

**解决方案**：
1. 检查事件订阅 → 确保有 `im:message.p2p_msg`
2. 重启网关：`openclaw gateway restart`
3. 检查环境变量：`type C:\Users\Komorebi\.openclaw\.env`

### ❌ 问题3：找不到机器人
**可能原因**：
1. 应用没发布
2. 只在测试环境

**解决方案**：
1. 飞书开放平台 → 版本管理与发布
2. 创建版本 → 申请发布
3. 等待审核（企业自建应用通常秒过）

---

## 📞 调试帮助

如果以上步骤都失败，请提供以下信息：

### 需要的信息（隐藏敏感部分）：
1. **飞书配置截图**（隐藏App Secret后几位）
2. **OpenClaw日志**：`openclaw log --tail 50`
3. **网关状态**：`openclaw gateway status`
4. **具体问题描述**：哪一步出错？错误信息？

### 快速诊断命令：
```powershell
# 1. 检查网关
openclaw gateway status

# 2. 检查日志
openclaw log --tail 30

# 3. 检查插件
openclaw plugins list
```

---

## ✅ 成功标志

配置成功后，你应该能看到：

### 在飞书里：
1. 机器人头像**彩色**（在线）
2. 私聊消息显示**“已读”**
3. 能收到我的回复

### 在OpenClaw里：
1. 日志显示接收消息
2. 飞书会话出现在 `sessions_list`
3. 能正常使用 `feishu_doc` 等工具

---

## 🆘 紧急求助

如果卡在某一步，可以：
1. **截图当前页面**（隐藏敏感信息）
2. **复制错误信息**
3. **联系我**，提供以上信息

---

**配置时间**：2026-03-19  
**预计成功时间**：20分钟内  
**难度等级**：中等（需要仔细操作）  

**提示**：建议**逐步操作**，完成一步检查一步，不要一次性操作所有步骤。